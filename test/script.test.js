process.env['SZN_DEBUG'] = true
import assert from 'assert'
import { assert as chaiAssertion } from 'chai'

suite('', () => {
    suite('', () => {
        test('Should .', () => {
          assert(true, '• Error')
        })        
    })
})
